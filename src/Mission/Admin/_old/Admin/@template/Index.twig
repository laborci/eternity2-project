{% extends('@smartpage/smartpage.twig') %}
{% block content %}
	<px-admin admin-title="Eternity" icon="fas fas fa-gamepad" user="{{ user.name }}" avatar="{{ user.getAdminAvatar() }}"></px-admin>
{% endblock %}